name: ephemeral-environments
services:
  server:
    container_name: webapp
    build:
      context: .
      args:
        GO_VERSION: 1.19
        HUGO_VERSION: 0.114.1
    volumes:
      - .:/app
    ports:
      - 1313:1313
